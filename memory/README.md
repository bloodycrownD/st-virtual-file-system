# 记忆（Memory）索引

本目录用于沉淀项目“可复用知识/结论/决策”，并区分：

- **持久记忆**：长期有效（见 `memory/persistence/`，按日期归档）
- **临时记忆**：阶段性跟踪（见 `memory/tmp/`，随阶段结束清理/归档）

说明：

- 原 `docs/` 中的知识类文档已迁移合并到本目录统一维护（`docs/` 仅保留指向说明）。

快速入口：

- `memory/persistence/20260506/index.md`
- `memory/tmp/index.md`

