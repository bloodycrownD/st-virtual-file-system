# VFS 项目交接快照

## 日期

2026-05-06

## 背景

当前会话已从 `st-better-database` 派生并初始化了新的 VFS 项目。用户计划清空会话记忆后，基于 memory 重新开始开发。

## 结论 / 事实

- 新项目已创建：`D:/Dev/Js/SillyTavern/public/scripts/extensions/third-party/st-virtual-filesystem`
- 新项目已完成脚手架初始化（Vite + TypeScript），并改造成 SillyTavern 扩展骨架：
  - 已有 `manifest.json`，`js` 指向 `dist/index.js`
  - `src/main.ts` 已改为挂载到 `#extensions_settings`
  - 已补齐 `vite.config.ts`、`vitest.config.ts`、`test/setup.ts`
  - `package.json` 已补充 `test/test:ui/test:run`
- 新项目已迁移文档：
  - `docs/` 已从旧项目拷贝
  - `memory/` 已从旧项目拷贝
- 新项目验证结果：
  - `npm run build` 通过
  - `npm run test:run` 通过

## 影响 / 下一步

- 清空会话后，请在 `st-virtual-filesystem` 目录重新开始，不要继续在 `st-better-database` 里实现 VFS。
- 下一阶段建议顺序：
  1. 固化 `VFS-核心数据模型.md`
  2. 实现 VFS Core Service（namespace/tree/file/version）
  3. 实现命令 API（read/write/append/patch/mv/rm）
  4. 最后补最小 UI

